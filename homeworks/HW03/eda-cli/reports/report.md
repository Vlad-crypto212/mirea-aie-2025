# EDA Report

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.60**
- Макс. доля пропусков по колонке: **0.00%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**

- Постоянные столбцы: **False**
- Категориальные признаки с высокой кардинальностью: **True**
- Подозрительные дубликаты ID: **True**
- Много нулевых значений: **False**

  - Признаки с высокой кардинальностью:  avg_session_duration_min
  - Столбцы с дубликатами ID: user_id
## Параметры отчета

- Максимальное количество гистограмм: **6**
- Количество топ-категорий: **10**
- Порог доли пропусков: **50.00%**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
